package com.web.medicine.baoanmedicine.enums;

public enum AdSlot {
    HOMEPAGE_BANNER,    // Banner to ở trang chủ
    PRODUCT_SIDEBAR,    // Cột bên cạnh trang sản phẩm
    SEARCH_RESULT,      // Hiển thị giữa các kết quả tìm kiếm
    CART_PAGE           // Hiển thị ở trang giỏ hàng (Gợi ý mua thêm)
}